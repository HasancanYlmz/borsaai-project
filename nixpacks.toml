providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m pip install -U pip", "python -m pip install -r requirements.txt"]

[start]
cmd = "python Backend/main.py"
